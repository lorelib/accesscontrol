<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lorelib.hawk.accesscontrol.domain.RoleRepository">
    <insert id="addUser">
        INSERT INTO ac_user (
        id,
        loginId,
        createBy,
        createDate
        ) VALUES (
        #{id},
        #{loginId},
        #{createBy},
        #{createDate}
        );
    </insert>
    
    <select id="getUser"
</mapper>